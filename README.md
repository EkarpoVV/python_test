1. You need create a folder
2. Run command in this folder ```git init```
Create a file
Edit the file (optional)
Create repositories in GIT HUB
You need sinc two repositories via git remote add NEW_LOCAL_REPOSITORY SSH_LINK e.g. git remote add NEW7 git@github.com:EkarpoVV/NEW7.git
Chek thet two repositories are synchronized via git remote -v
Run git add
Create commit via git commit -m 'Add Readmy file'
Sent file to GitHUB via git push -u NEW7 main OR git puch -u NEW7 master
